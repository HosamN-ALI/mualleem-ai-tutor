# ⚙️ دليل التشغيل والعمليات لمنصة «معلّم» (Operational Guide)

> هذا الدليل موجّه لفريق التشغيل (Operations / Support) والمشرفين التربويين الذين يستخدمون منصة **معلّم** بعد تثبيتها، ويركّز على:
> - كيفية رفع المناهج الدراسية (PDF) وفهرستها في Qdrant  
> - كيفية استخدام واجهة الدردشة (Chat Interface)  
> - نظرة عملية على الـ API Endpoints الرئيسية  
> - إجراءات الصيانة والمتابعة الدورية  

---

## 1. 📚 رفع وفهرسة ملفات المناهج (Curriculum PDFs)

محرك RAG في منصة «معلّم» يعتمد على رفع ملفات PDF لمقررات الرياضيات (أو غيرها)، ثم تقسيمها إلى مقاطع (Chunks) وتخزينها في **Qdrant Cloud** لاستخدامها في الإجابة عن أسئلة الطلاب.

### 1.1 المتطلبات قبل رفع المناهج

قبل البدء:

1. تأكّد من أن:
   - خادم الـ Backend يعمل على: `http://localhost:8000`
   - اتصال Qdrant Cloud سليم (`test_qdrant.py` ناجح)
   - إعدادات Requesty.ai صحيحة (`test_requesty.py` ناجح)
2. يُفضّل أن تكون ملفات الـ PDF:
   - نصية قدر الإمكان (ليست صوراً فقط)، أو على الأقل قابلة للـ OCR.
   - ملف لكل كتاب/منهج (مثلاً: `math_grade_7_term1.pdf`).

### 1.2 رفع ملف منهج باستخدام HTTP API

الـ Endpoint المخصّص للرفع هو:  
`POST /upload-curriculum`

#### 1.2.1 مثال باستخدام `curl`

```bash
curl -X POST http://localhost:8000/upload-curriculum \
  -F "file=@/path/to/textbook.pdf"
```

- `file=@/path/to/textbook.pdf`  
  مسار الملف الموجود على جهازك.

كما موضّح في:

- [`REQUESTY_SETUP_COMPLETE.md`](mualleem-ai-tutor/REQUESTY_SETUP_COMPLETE.md:222)  
- [`QDRANT_SETUP_EN.md`](mualleem-ai-tutor/QDRANT_SETUP_EN.md:176)  

#### 1.2.2 ماذا يحدث خلف الكواليس؟

عند استدعاء `/upload-curriculum`:

1. يتم استلام ملف الـ PDF في FastAPI.
2. يتم استخراج النص وتقسيمه إلى مقاطع نصية صغيرة (Chunks).
3. يُنشئ النظام تمثيلاً متجهيًا (Embeddings) لكل مقطع باستخدام:
   - `openai/text-embedding-3-small` (عبر Requesty.ai).
4. تُخزن هذه المتجهات في **Qdrant Collection** (مثل `mualleem_curriculum`).
5. يتم ربط كل مقطع بمعلومات مثل:
   - رقم الصفحة
   - رقم الفصل (إن توفّر)
   - اسم الكتاب

#### 1.2.3 التحقق بعد الرفع

يمكن التحقق من حالة البيانات عبر:

```bash
curl http://localhost:8000/stats
```

من [`QDRANT_SETUP_EN.md`](mualleem-ai-tutor/QDRANT_SETUP_EN.md:176) يتوقّع أن يعرض:

- اسم الـ Collection
- عدد الـ Chunks المخزّنة
- حجم المتجه (Vector size = 1536)
- حالة الاتصال بـ Qdrant

---

## 2. 💬 استخدام واجهة الدردشة (Chat Interface)

واجهة الدردشة مبنية باستخدام **Next.js** في مجلّد `frontend/` كما هو موثّق في [`FRONTEND_COMPLETE.md`](mualleem-ai-tutor/FRONTEND_COMPLETE.md:1).

### 2.1 الوصول إلى واجهة الدردشة

بعد تشغيل الـ Frontend:

```bash
cd frontend
npm run dev
```

- افتح المتصفح على: `http://localhost:3000`

ستظهر واجهة الدردشة مع:

- حقل نصي لكتابة السؤال بالعربية.
- زر لرفع صورة (مسألة مكتوبة / صورة صفحة من كتاب).
- منطقة رسائل (Chat History) تظهر فيها أسئلة الطالب وردود المعلّم الذكي.

### 2.2 مكوّنات الواجهة الرئيسية

من [`FRONTEND_COMPLETE.md`](mualleem-ai-tutor/FRONTEND_COMPLETE.md:9):

- [`components/ChatInterface.tsx`](mualleem-ai-tutor/frontend/components/ChatInterface.tsx:1)
  - حقل إدخال نص
  - زر رفع صورة
  - زر إرسال
  - تكامل مع `/chat` في الـ Backend
- [`components/ChatBubble.tsx`](mualleem-ai-tutor/frontend/components/ChatBubble.tsx:1)
  - عرض رسائل المستخدم والـ Assistant
  - عرض المعادلات باستخدام LaTeX (`react-katex`)
  - دعم RTL للعربية

### 2.3 آلية عمل الدردشة (Text + Image)

عند إرسال سؤال من واجهة المستخدم:

1. يتم بناء طلب `multipart/form-data` يحتوي على:
   - `question`: نص السؤال بالعربية.
   - `image`: (اختياري) صورة للمسألة.

2. يتم إرسال الطلب إلى الـ Backend عبر:

   ```http
   POST http://localhost:8000/chat
   ```

   كما مذكور في [`FRONTEND_COMPLETE.md`](mualleem-ai-tutor/FRONTEND_COMPLETE.md:75).

3. في الـ Backend:
   - يتم استدعاء RAG Engine للبحث في Qdrant:
     - لاسترجاع المقاطع ذات الصلة من المناهج المرفوعة.
   - يتم إرسال السياق + السؤال إلى نموذج OpenAI (عبر Requesty.ai) مثل:
     - `openai/gpt-4o-mini` للنصوص
     - `openai/gpt-4o` للصور + النص
   - يتم توليد إجابة بالعربية مع دعم LaTeX.

4. النتيجة تعاد في شكل JSON:

   ```json
   {
     "answer": "الإجابة بالعربية مع LaTeX: $x^2 + y^2 = r^2$"
   }
   ```

5. يعرض الـ Frontend الإجابة في فقاعة الدردشة مع تنسيق المعادلات.

### 2.4 استخدام الواجهة عمليًا

#### 2.4.1 سؤال نصي بسيط

1. افتح `http://localhost:3000`
2. اكتب:  
   **"ما هو قانون فيثاغورس؟"**
3. اضغط "إرسال".
4. ينبغي أن تعود إجابة تتضمن:  
   - شرح بالنص العربي.
   - معادلة مثل:  
     `a^2 + b^2 = c^2`

#### 2.4.2 سؤال مع صورة

1. اضغط زر رفع الصورة.
2. اختر صورة لمسألة رياضية (JPG/PNG).
3. اكتب سؤال مثل:  
   "ما هو حل هذه المسألة؟"
4. اضغط "إرسال".
5. سيقوم النظام بتمرير الصورة للنموذج `gpt-4o` عبر Requesty.ai وتحليل الصورة مع النص.

---

## 3. 🌐 توثيق الـ API Endpoints

في ملفات التوثيق مثل [`REQUESTY_SETUP_COMPLETE.md`](mualleem-ai-tutor/REQUESTY_SETUP_COMPLETE.md:86) و[`QDRANT_SETUP_EN.md`](mualleem-ai-tutor/QDRANT_SETUP_EN.md:122) تم توثيق الـ API كالتالي:

### 3.1 قائمة الـ Endpoints الرئيسية

| Endpoint              | Method | الوصف                                                         |
|----------------------|--------|----------------------------------------------------------------|
| `/`                  | GET    | صفحة رئيسية بسيطة / Root                                     |
| `/health`            | GET    | فحص صحة الخادم (Health Check)                               |
| `/stats`             | GET    | إحصائيات الـ RAG/Qdrant (عدد المقاطع، حالة الاتصال...)      |
| `/upload-curriculum` | POST   | رفع ملف PDF وفهرسته في Qdrant                               |
| `/chat`              | POST   | محادثة مع المعلّم الذكي (سؤال نصي + صورة اختيارية)          |

### 3.2 تفاصيل كل Endpoint

#### 3.2.1 `GET /health`

- **الغرض**: التحقق من أن الخادم يعمل بشكل صحيح.
- **الاستدعاء**:

  ```bash
  curl http://localhost:8000/health
  ```

- **استجابة متوقعة** (مثال من [`QDRANT_SETUP_EN.md`](mualleem-ai-tutor/QDRANT_SETUP_EN.md:80)):

  ```json
  {
    "status": "healthy",
    "service": "Mualleem Backend"
  }
  ```

#### 3.2.2 `GET /stats`

- **الغرض**: عرض إحصائيات الـ Collection في Qdrant.
- **الاستدعاء**:

  ```bash
  curl http://localhost:8000/stats
  ```

- **المخرجات**:  
  JSON يحتوي على:
  - اسم الـ Collection
  - عدد الوثائق/Chunks
  - إعدادات المتجه (vector size, distance)

#### 3.2.3 `POST /upload-curriculum`

- **الغرض**: رفع منهج PDF جديد وفهرسته.
- **المدخلات**:
  - `file`: ملف PDF (multipart/form-data).
- **مثال**:

  ```bash
  curl -X POST http://localhost:8000/upload-curriculum \
    -F "file=@/path/to/textbook.pdf"
  ```

- **النتيجة**:
  - رسالة نجاح وفهرسة في Qdrant.
  - زيادة في عدد الـ Chunks في `/stats`.

#### 3.2.4 `POST /chat`

- **الغرض**: إرسال سؤال نصي (مع صورة اختيارية) والحصول على إجابة من المعلّم الذكي.
- **المدخلات (multipart/form-data)**:
  - `question`: نص السؤال (إجباري).
  - `image`: ملف صورة للمسألة (اختياري).
- **مثال (نصي فقط)**:

  ```bash
  curl -X POST http://localhost:8000/chat \
    -F "question=اشرح لي نظرية فيثاغورس"
  ```

- **مثال (نص + صورة)** من [`REQUESTY_SETUP_COMPLETE.md`](mualleem-ai-tutor/REQUESTY_SETUP_COMPLETE.md:234):

  ```bash
  curl -X POST http://localhost:8000/chat \
    -F "question=ما هو حل هذه المسألة؟" \
    -F "image=@/path/to/math_problem.jpg"
  ```

- **استجابة متوقعة** (تبسيط):

  ```json
  {
    "answer": "شرح مفصّل بالعربية مع خطوات الحل ومعادلات LaTeX"
  }
  ```

---

## 4. 🛠 إجراءات الصيانة الدورية (Maintenance Procedures)

### 4.1 مراقبة Requesty.ai

من [`REQUESTY_SETUP_COMPLETE.md`](mualleem-ai-tutor/REQUESTY_SETUP_COMPLETE.md:173):

- قم بتسجيل الدخول إلى: https://app.requesty.ai
- يمكنك من هناك:
  - عرض إحصائيات الاستخدام (عدد الطلبات، النماذج المستخدمة).
  - مراقبة التكاليف لكل نموذج.
  - ضبط حدود الاستخدام والتنبيهات.

**توصية تشغيلية:**

- استخدم النموذج `openai/gpt-4o-mini` لأغلب الأسئلة النصية لتقليل التكلفة.
- احتفظ بـ `openai/gpt-4o` للمهام التي تعتمد على الصور (Vision).

### 4.2 مراقبة Qdrant Cloud

- من لوحة Qdrant Cloud يمكنك:
  - التحقق من حالة الـ Cluster.
  - مراقبة حجم البيانات.
  - التأكد من وجود الـ Collection (مثل `mualleem_curriculum`).

**مهام دورية مقترحة:**

- مراجعة النمو المتزايد في عدد الـ Chunks عند إضافة مناهج جديدة.
- حذف البيانات القديمة أو إعادة الفهرسة إن تغيّرت المناهج.

### 4.3 تحديث التبعيات (Dependencies)

#### 4.3.1 Backend

من وقت لآخر:

```bash
cd backend
source .venv/bin/activate
pip install -r requirements.txt --upgrade
```

#### 4.3.2 Frontend

```bash
cd frontend
npm install
npm audit fix   # اختياري للتنبيهات الأمنية
```

(مذكور في [`FRONTEND_COMPLETE.md`](mualleem-ai-tutor/FRONTEND_COMPLETE.md:219) وجود تنبيه أمني يمكن معالجته بـ `npm audit fix`.)

### 4.4 التحقق الشامل (Verification Checklist)

استخدم ملف [`VERIFICATION_CHECKLIST.md`](mualleem-ai-tutor/VERIFICATION_CHECKLIST.md:300) كمرجع نهائي للتأكد من:

- عمل رفع المناهج.
- عمل الدردشة النصية.
- عمل الدردشة مع الصور.
- عمل التكامل مع Requesty.ai وQdrant Cloud.

---

## 5. 🧹 مهام إدارية إضافية

### 5.1 إعادة فهرسة منهج (Re-indexing)

في حال تحديث كتاب أو تعديل منهج:

1. احذف الـ Collection القديمة من Qdrant (اختياري / من خلال لوحة Qdrant أو سكربت خاص).
2. أعد رفع ملف الـ PDF المحدّث باستخدام `/upload-curriculum`.
3. تأكد من `/stats` أن عدد الـ Chunks يعكس البيانات الجديدة.

### 5.2 التعامل مع مشاكل الأداء

إذا لاحظت بطئًا في الاستجابة:

1. تحقّق من:
   - زمن استجابة Qdrant Cloud.
   - زمن استجابة Requesty.ai.
2. راقب:
   - عدد الـ Chunks لكل Collection (الكتب الكثيرة قد تتطلب تحسين إعدادات التقسيم).
3. ناقش مع فريق التطوير احتمالية:
   - تحسين طريقة تقسيم النص.
   - استخدام نماذج أرخص/أسرع لـأسئلة بسيطة.

---

## 6. 📎 ملخص تشغيلي سريع (Ops Quick Checklist)

1. **بعد تشغيل النظام:**
   - تأكد من نجاح `/health`.
   - تأكد من أن `/stats` تعرض اتصالاً صحيحاً بـ Qdrant.

2. **لإضافة منهج جديد:**
   - استخدم `POST /upload-curriculum` مع ملف PDF.
   - تحقق من `/stats` بعد الرفع.

3. **لمساعدة الطلاب:**
   - افتح واجهة الدردشة على `http://localhost:3000`.
   - شجّع الطلاب على كتابة الأسئلة بالعربية ورفع صور المسائل.

4. **للمتابعة والمراقبة:**
   - راقب `Requesty.ai` للـ Usage & Cost.
   - راقب Qdrant Cloud لحالة البيانات والأداء.

---

**بهذا الدليل يستطيع فريق التشغيل إدارة منصة «معلّم» بكفاءة: رفع المناهج، تشغيل الدردشة، مراقبة الأداء، وضمان استمرارية الخدمة للطلاب والمعلمين.**  